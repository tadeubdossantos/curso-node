// possibilita exportar este módulo
module.exports = {
    /* é uma função mas não precisamos incluir 'function',
    pois já entende que se trata de função, deixando 
    a sintaxe reduzida */
    soma(a,b) {
        console.log(a+b);
    } 
}